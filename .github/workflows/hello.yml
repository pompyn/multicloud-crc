name: Hello Action
on:
push:
branches: [ main ]
workflow_dispatch:
jobs:
hello:
runs-on: ubuntu-latest
steps:
- uses: actions/checkout@v4
- run: echo "Hello from Github Actions as $GITHUB_ACTOR on $GITHUB_REF"
